# Information Architecture — {feature-name}

> **Generated from**: docs/prd/PRD_{feature-name}.md
> **Created**: {YYYY-MM-DD}
> **Status**: Draft

## 1. Page Hierarchy

```mermaid
mindmap
  root(({feature-name}))
    진입
      / (Landing)
    작성
      /submit (작성 폼)
      /my (본인 목록)
    관리
      /admin (관리자)
      /admin/stats (통계)
```

**원칙**:
- 1Depth ≤ 7개 (Miller's Law)
- 모든 페이지는 진입 경로가 있어야 함
- 고아 페이지(어디서도 접근 불가) 금지

## 2. Page × Functional Requirement Matrix

| Route | FR Coverage | Primary FR | Secondary FRs |
|-------|------------|-----------|---------------|
| `/` | FR-001 | Magic Link 인증 | - |
| `/submit` | FR-002, FR-003, FR-004 | 작성 폼 | 필드 검증, 인라인 가이드 |
| `/my` | FR-005 | 본인 작성 목록 | 수정 기능 |
| `/admin` | FR-006, FR-007 | 관리자 화면 | 익스포트 |
| `/admin/stats` | FR-012 | 부서별 진행률 | - |

**규칙**:
- 모든 FR이 1+ 페이지에 매핑되어야 함
- 페이지가 0개의 FR과 연결되면 → 페이지가 불필요하거나 FR이 누락
- API/Backend 전용 FR은 매핑 테이블에서 제외 (별도 명시)

## 3. Audience × Page Access Matrix

| Route | guest | author | admin |
|-------|-------|--------|-------|
| `/` | ✓ | ✓ | ✓ |
| `/submit` | - | ✓ | ✓ |
| `/my` | - | ✓ | ✓ |
| `/admin` | - | - | ✓ |
| `/admin/stats` | - | - | ✓ |

- `✓`: 접근 허용
- `-`: 접근 시 no-permission 상태로 분기 (또는 `/`로 리다이렉트)

## 4. Navigation Structure

### Primary Navigation (로그인 후)

```
┌──────────────────────────────────────┐
│ Logo  |  내 작성  |  관리(admin only)  │
└──────────────────────────────────────┘
```

### Secondary Navigation

- Breadcrumb: `홈 > 관리 > 통계`
- Footer: 도움말, 문의, 로그아웃

## 5. Open Questions

화면별 명세 진입 전 결정 필요:
- [ ] /admin에서 통계 페이지로 가는 진입점은 사이드바인가 탭인가?
- [ ] 모바일에서 admin 페이지는 별도 화면인가 desktop-only인가?
